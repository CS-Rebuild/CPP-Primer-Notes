#include <vector>
#include "Sales_data.h"
using namespace std;

int main() {
    Sales_data item1, item2, sum;
    while (cin >> item1 >> item2) {
            sum = item1 + item2;
    }

    return 0;
}
