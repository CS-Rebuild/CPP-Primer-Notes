#include <iostream>
using namespace std;

int main() {
    double dval; int ival; int *pi;
    //dval = ival = pi = 0;
    //pi = ival = dval = 0;
    //dval = ival = (int)pi = 0;
    //dval = ival = (int)(pi = 0);
    dval = ival = 0;
    pi = 0;

    return 0;
}
